# v0.9.0 Release Gate

**Status:** PENDING
**Spec:** `docs/architecture/2026-05-11-v0.9.0-destination.md § 7 (Tier 7)`  
**Runner:** `./scripts/v0.9.0-release-gate.sh`  
**Last run:** 2026-05-12 20:13:47 UTC

This document records the six PASS criteria for v0.9.0. Each criterion must show
PASS before `git tag v0.9.0` is pushed. The gate-runner script (`scripts/v0.9.0-release-gate.sh`)
populates the PASS/PENDING/FAIL annotations automatically. Manual override is allowed
but must include a signed-off operator note in the Evidence section.

---

## Criterion 1 — Composer selects ≥2 distinct roles

**Description:**  
`composeExperts(spec, context)` must select at minimum two distinct role IDs when
given a typical feature spec. This validates that role composition actually fans out
across multiple advisors rather than collapsing to a single pass.

**How to verify:**  
1. Run the gate script: `./scripts/v0.9.0-release-gate.sh`  
   The script invokes `composeExperts` with the bundled test spec and logs the selected
   role list.  
2. Manually: load a spec via `brainstorming` or `writing-plans` and observe the
   expert-turn log (or sidecar `role_sessions` entries). Count distinct `requested_role`
   values across the top-level dispatch.  
3. Programmatic check: `node -e "import('./lib/codex-bridge/role-composer.js').then(m=>console.log(m.composeExperts(require('./tests/smoke/live-verification-fixture/spec.md'))))"` — expect array with length ≥ 2.

**Result:** PENDING

**Evidence:**  
PENDING — CPS_INSTALLED_SMOKE not set; live verification not run.

---

## Criterion 2 — Distinct CLIs route per role (cross-model dispatch verified)

**Description:**  
Each selected role must resolve to a distinct CLI adapter. Specifically, at least two
roles must dispatch to different CLIs (e.g., `codex` for one role and `claude` or
`ollama` for another). The sidecar `resolved_cli` field must differ across role turns.
This confirms the routing ladder works — not that every role picks a unique CLI, but
that the system is not trivially pinned to a single adapter.

**How to verify:**  
1. Run `./scripts/v0.9.0-release-gate.sh` — the script captures `resolved_cli` from
   each turn's resolution-audit block and asserts at least two distinct values.  
2. Manually: after running any skill that dispatches experts, inspect the sidecar file
   (`cat $(find .superpowers-codex-paired -name '*.json') | jq '.role_sessions[].turns[].resolved_cli'`).
   Distinct values must appear.  
3. Doctor output: `node lib/codex-bridge/cli.js doctor` must show at minimum two CLIs
   as "available" so the routing ladder has candidates to choose from.

**Result:** PENDING

**Evidence:**  
PENDING — CPS_INSTALLED_SMOKE not set; live verification not run.

---

## Criterion 3 — Sidecar audit fields fully populated per turn

**Description:**  
Every expert-turn record in the sidecar must contain the full resolution-audit block:
`requested_role`, `resolved_cli`, `resolution_source`, `preference_index`,
`preference_ladder`, `unavailable_candidates`, `fallback_reason`.  
Additionally: `adapter`, `inputs_hash`, `response_hash`, `role_prompt_version`,
`role_prompt_hash` must all be present and non-null (where applicable).  
The `panel_id` field is required when the turn was part of a panel dispatch.

**How to verify:**  
1. Run `./scripts/v0.9.0-release-gate.sh` — reads sidecar JSON and checks each turn
   with `jq` for required field presence.  
2. Manually: after a skill run, open the sidecar and verify:  
   ```bash
   cat .superpowers-codex-paired/path/to/spec.md.json | \
     jq '.role_sessions[].turns[] | {requested_role, resolved_cli, resolution_source, adapter, inputs_hash, response_hash}'
   ```  
   All six fields must be non-null strings.  
3. For panel turns, additionally check `panel_id` is present and is a string matching
   the panel dispatch UUID.

**Result:** PENDING

**Evidence:**  
PENDING — CPS_INSTALLED_SMOKE not set; live verification not run.

---

## Criterion 4 — Peer DM round-tripped across adapters

**Description:**  
At least one peer direct-message must be enqueued by one expert role and consumed
by a different role. The sidecar `mailbox_message_ids` for the receiving turn must
reference the sent message ID. This proves the mailbox injection hook fires correctly
in worktree subagent contexts and that cross-adapter DM delivery is not silently
dropped.

**How to verify:**  
1. Run `./scripts/v0.9.0-release-gate.sh` — the script traces mailbox state before
   and after a dual-CLI dispatch, then checks the recipient turn's `mailbox_message_ids`.  
2. Manually: after running `subagent-driven-development` or `test-driven-development`
   with two or more expert roles, inspect the sidecar:  
   ```bash
   cat .superpowers-codex-paired/path/to/spec.md.json | \
     jq '.role_sessions[].turns[] | select(.mailbox_message_ids | length > 0) | {requested_role, mailbox_message_ids}'
   ```  
   At least one turn must show a non-empty `mailbox_message_ids` list.  
3. Cross-adapter proof: the sending turn's `resolved_cli` and the receiving turn's
   `resolved_cli` must differ (DM crossed adapter boundaries).

**Result:** PENDING

**Evidence:**  
PENDING — CPS_INSTALLED_SMOKE not set; live verification not run.

---

## Criterion 5 — Double-SHIP convergence within 7-round budget on a real spec

**Description:**  
Running `autopilot` (or the equivalent `writing-plans` → `subagent-driven-development`
cycle) on the bundled test spec must reach a SHIP verdict from both Claude and the
routed expert CLI within at most 7 rounds. Both advisors must emit `SHIP` (or the
equivalent approval verdict) before the round budget is exhausted. The sidecar must
record the final `verdict: "SHIP"` per-round entry.

**How to verify:**  
1. Run `./scripts/v0.9.0-release-gate.sh` — runs a constrained `autopilot` pass on
   the bundled test spec (`tests/smoke/live-verification-fixture/spec.md` or the script's
   inline spec). The script counts rounds to SHIP and asserts ≤ 7.  
2. Manually: run `brainstorming` or `autopilot` on any small spec; observe the round log
   in the sidecar:  
   ```bash
   cat .superpowers-codex-paired/path/to/spec.md.json | \
     jq '[.rounds[] | {round: .round_number, verdict: .verdict}]'
   ```  
   The final round must have `"verdict": "SHIP"` and `round_number` ≤ 7.  
3. If convergence fails within 7 rounds: the gate FAILS (do not adjust the budget
   without a documented justification reviewed on the Codex thread).

**Result:** PENDING

**Evidence:**  
PENDING — CPS_INSTALLED_SMOKE not set; live verification not run.

---

## Criterion 6 — Replay reconstructs identical inputs_hash from sidecar

**Description:**  
Given any completed expert-turn sidecar entry, `replayTurn(turn, deps)` must
reconstruct the assembled prompt and produce an `inputs_hash` that is byte-identical
to the recorded value. "Byte-identical" means exact string equality, not just hash
collision resistance. This validates the stateless-replay invariant that makes
sidecar entries the canonical source of truth for audit.

**How to verify:**  
1. Run `./scripts/v0.9.0-release-gate.sh` — after completing a round, it calls
   `replayTurn` against the first expert turn and asserts `result.inputsHashMatches === true`
   AND that `result.assembledPrompt` contains the same role ID, role-prompt content,
   and spec snippet that were recorded.  
2. Manually: run `node --test tests/replay/replay-from-sidecar.test.js` — all
   replay tests must pass including the byte-identity assertion.  
3. Cross-CLI variant: record a turn under the `codex` adapter, then replay under
   `claude-task`. The `inputs_hash` must still match (adapter is not in the hash
   domain); only a warning about adapter mismatch should appear, not a FAIL.

**Result:** PASS

**Evidence:**  
PASS — node --test tests/replay/replay-from-sidecar.test.js — all tests green. inputs_hash byte-identity verified. Cross-CLI mismatch warning verified.

---

## Gate summary

| # | Criterion | Result |
|---|-----------|--------|
| 1 | — | PENDING |
| 2 | — | PENDING |
| 3 | — | PENDING |
| 4 | — | PENDING |
| 5 | — | PENDING |
| 6 | — | PASS |

**Overall:** PENDING — last run 2026-05-12 20:13:47 UTC

---

## Operator notes

_(Space for operator sign-off, manual overrides, and exception rationale. Any manual
PASS override must include: operator name, date, command run, and output excerpt.)_
