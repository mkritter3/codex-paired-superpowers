{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-commit-provenance.sh",
        "matcher_args": {
          "command": ".*git\\s+commit.*"
        }
      }
    ]
  }
}
