{
  "description": "codex-paired-superpowers provenance hook: verifies commit conventions during active autopilot runs",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/check-commit-provenance.sh\""
          }
        ]
      }
    ]
  }
}
